[package]
name = "abi-fixture-echo"
version = "0.1.0"
description = "测试夹具：一个真实编译为动态库的附加模块插件，用于端到端验证 helper 加载链路"
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
copper-module-abi = { path = "../../copper-module-abi" }
serde_json = "1"
