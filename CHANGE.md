# 更新日志

本文件记录铜傀儡启动器（铜内核 CopperCore）的版本变更。

版本号遵循语义化约定 `x.y.z`：
- 第 1 位保持 `0`；
- 第 2 位为大版本，表示重要更新；
- 第 3 位为小版本，修复 bug 或小改动时更新。

版本条目自上而下排列，最新版在最上。

---

## [0.1.0] - 待发布

### 新增

- 建立铜内核（CopperCore）Tauri v2 工程骨架：
  - Rust 后端（`src-tauri/`）与 Vue 3 前端（`frontend/`）分层。
  - 本地构建链路验证通过（前端构建 + Rust release 构建）。
- 初始化 GitHub 持续集成：
  - `build` 工作流：提交 / 拉取请求校验可构建性。
  - `release` 工作流：标签 `v*` 自动触发，或 Actions 手动填写版本 tag 触发发布。
- 仓库规范文档初始化：README（中 / 英）、更新日志、GPL-3.0 许可、贡献 / 安全 / 支持与问题与拉取请求模板。

> 首个正式版本发布时将回填具体功能条目，并依据《更新版本流程》调整版本号 / 变更 / 文档。